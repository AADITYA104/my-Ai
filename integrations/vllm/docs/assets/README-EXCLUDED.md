This merge removed docs/assets/ (~33MB of documentation images/diagrams) —
purely illustrative for the docs website, not needed to install or run
vLLM. Left out for the same reason as the other size-trims documented
elsewhere in this project (see /INTEGRATIONS.md). All actual docs text
(.md files) is kept in full.
